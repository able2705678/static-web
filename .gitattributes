# 资源文件标记为二进制（不使用 Git LFS）
# 说明：本仓库图片/音频/压缩包均 < 4MB，远低于 GitHub 100MB 单文件限制，无需 LFS。
# 此前 UGit 自动生成的 LFS 规则把这些小文件纳入 LFS，导致单次推送批量对象超过
# GitHub 的 100 上限而推送失败；现改为普通二进制跟踪即可正常推送。
*.png binary
*.jpg binary
*.mp3 binary
*.zip binary
